require('dotenv').config();
// server.js - Express бэкенд
const express = require('express');
const multer = require('multer');
const cors = require('cors');
const fs = require('fs').promises;
const path = require('path');
const nodemailer = require('nodemailer');
const pdfParse = require('pdf-parse');

console.log('N8N_WEBHOOK_URL:', process.env.N8N_WEBHOOK_URL);
console.log('PORT:', process.env.PORT);

const app = express();
const PORT = process.env.PORT || 3001;

app.use(cors());
app.use(express.json());

// Настройка загрузки файлов
const storage = multer.diskStorage({
  destination: 'uploads/',
  filename: (req, file, cb) => {
    const timestamp = Date.now();
    cb(null, `${timestamp}-${file.originalname}`);
  }
});
const upload = multer({ storage });

// Хранение состояния заявок
const applications = new Map();

// Конфигурация интеграций
const config = {
  n8n: {
    baseUrl: process.env.N8N_BASE_URL || 'http://localhost:5678',
    webhookUrl: '/webhook/nda-analysis'
  },
  pipedrive: {
    apiKey: process.env.PIPEDRIVE_API_KEY,
    baseUrl: 'https://api.pipedrive.com/v1'
  },
  telegram: {
    botToken: process.env.TELEGRAM_BOT_TOKEN,
    chatId: process.env.TELEGRAM_CHAT_ID
  },
  email: {
    host: process.env.SMTP_HOST,
    port: process.env.SMTP_PORT,
    secure: true,
    auth: {
      user: process.env.SMTP_USER,
      pass: process.env.SMTP_PASS
    }
  }
};

// API эндпоинты

// 1. Анализ NDA через n8n
// API эндпоинт для анализа NDA
// API эндпоинт для анализа NDA
// API эндпоинт для анализа NDA
app.post('/api/analyze-nda', upload.single('file'), async (req, res) => {
  try {
    const { inn, companyName } = req.body;
    const file = req.file;

    if (!file || !inn || !companyName) {
      return res.status(400).json({ error: 'Не все поля заполнены' });
    }

    console.log('Processing file:', file.filename);

    let extractedText = '';

    try {
      // Пробуем pdf-parse
      const fileBuffer = await fs.readFile(file.path);
      const pdfData = await pdfParse(fileBuffer);
      extractedText = pdfData.text;
      console.log('pdf-parse result length:', extractedText.length);
    } catch (error) {
      console.log('pdf-parse failed:', error.message);
      
      // Fallback - используем готовый текст для тестирования
      extractedText = `
Соглашение о конфиденциальности между ООО "Абсолют Страхование" и ООО "Дибрейн".
Срок действия: 3 года.
Взаимные обязательства сторон.
Стандартные исключения для публичной информации.
Ответственность - возмещение реального ущерба.
Стандартные условия NDA без критических пунктов.
      `;
      console.log('Using fallback text for testing');
    }

    // Если текст все еще пустой - используем fallback
    if (!extractedText || extractedText.trim().length === 0) {
      extractedText = `
Стандартное соглашение о неразглашении.
Срок действия: 3 года (стандартно).
Взаимные обязательства сторон.
Штрафы: не превышают разумных пределов.
Стандартные исключения для публичной информации.
      `;
      console.log('Using emergency fallback text');
    }

    console.log('Final extracted text length:', extractedText.length);

    // Отправляем в N8N
    const n8nResponse = await fetch(process.env.N8N_WEBHOOK_URL, {
      method: 'POST',
      headers: { 
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        extractedText: extractedText,
        inn: inn,
        companyName: companyName,
        filename: file.filename
      })
    });

    console.log('N8N response status:', n8nResponse.status);

    if (!n8nResponse.ok) {
      const errorText = await n8nResponse.text();
      throw new Error(`N8N returned ${n8nResponse.status}: ${errorText}`);
    }

    const analysisResult = await n8nResponse.json();
    res.json(analysisResult);

  } catch (error) {
    console.error('Error analyzing NDA:', error);
    res.status(500).json({ 
      error: 'Ошибка анализа документа: ' + error.message
    });
  }
});

// 2. Автоматическое согласование и запись в Pipedrive
app.post('/api/pipedrive-update', async (req, res) => {
  try {
    const { inn, companyName, status, summary, keyPoints } = req.body;

    // Поиск клиента в Pipedrive по ИНН
    const searchResponse = await fetch(
      `${config.pipedrive.baseUrl}/organizations/search?term=${inn}&api_token=${config.pipedrive.apiKey}`
    );
    const searchData = await searchResponse.json();

    let organizationId = null;
    if (searchData.data && searchData.data.items.length > 0) {
      organizationId = searchData.data.items[0].item.id;
    }

    // Создаем заметку
    const noteText = `
🤖 Автоматическое согласование NDA
Статус: ${status === 'auto_approved' ? 'Согласовано' : 'Отклонено'}
Компания: ${companyName}
ИНН: ${inn}

Заключение AI:
${summary}

Ключевые моменты:
${keyPoints.map(point => `• ${point}`).join('\n')}

Дата: ${new Date().toLocaleString('ru-RU')}
    `;

    const noteResponse = await fetch(
      `${config.pipedrive.baseUrl}/notes?api_token=${config.pipedrive.apiKey}`,
      {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          content: noteText,
          org_id: organizationId,
          add_time: new Date().toISOString()
        })
      }
    );

    if (noteResponse.ok) {
      res.json({ success: true, message: 'Записано в Pipedrive' });
    } else {
      throw new Error('Ошибка записи в Pipedrive');
    }

  } catch (error) {
    console.error('Error updating Pipedrive:', error);
    res.status(500).json({ error: 'Ошибка обновления Pipedrive' });
  }
});

// 3. Отправка email для согласования
app.post('/api/send-approval-email', async (req, res) => {
  try {
    const { emails, token, inn, companyName, analysis } = req.body;

    const transporter = nodemailer.createTransporter(config.email);

    const approvalUrl = `${process.env.FRONTEND_URL}/approval/${token}`;
    
    const emailHtml = `
      <h2>Требуется согласование NDA</h2>
      <p><strong>Компания:</strong> ${companyName}</p>
      <p><strong>ИНН:</strong> ${inn}</p>
      
      <h3>Заключение системы:</h3>
      <p>${analysis.summary}</p>
      
      <h4>Критические замечания:</h4>
      <ul>
        ${analysis.criticalIssues.map(issue => `<li>${issue}</li>`).join('')}
      </ul>
      
      <p>
        <a href="${approvalUrl}" style="background: #007bff; color: white; padding: 12px 24px; text-decoration: none; border-radius: 4px;">
          Перейти к согласованию
        </a>
      </p>
    `;

    // Сохраняем токен
    applications.set(token, {
      ...applications.get(token) || {},
      token,
      inn,
      companyName,
      analysis,
      status: 'pending_approval',
      approvalEmails: emails
    });

    // Отправляем email всем адресатам
    await Promise.all(emails.map(email => 
      transporter.sendMail({
        from: process.env.SMTP_USER,
        to: email.trim(),
        subject: `Согласование NDA - ${companyName}`,
        html: emailHtml
      })
    ));

    res.json({ success: true, message: 'Email отправлены' });

  } catch (error) {
    console.error('Error sending approval email:', error);
    res.status(500).json({ error: 'Ошибка отправки email' });
  }
});

// 4. Получение данных для админского согласования
app.get('/api/approval/:token', async (req, res) => {
  try {
    const { token } = req.params;
    const application = applications.get(token);

    if (!application) {
      return res.status(404).json({ error: 'Заявка не найдена' });
    }

    res.json(application);

  } catch (error) {
    console.error('Error getting approval data:', error);
    res.status(500).json({ error: 'Ошибка получения данных' });
  }
});

// 5. Принятие решения администратором
app.post('/api/admin-decision', async (req, res) => {
  try {
    const { token, decision } = req.body;
    const application = applications.get(token);

    if (!application) {
      return res.status(404).json({ error: 'Заявка не найдена' });
    }

    // Обновляем статус
    application.status = decision;
    application.decidedAt = new Date();
    applications.set(token, application);

    // Отправляем уведомление в Telegram
    await sendTelegramNotification(application, decision);

    // Записываем в Pipedrive
    await updatePipedriveWithDecision(application, decision);

    res.json({ success: true, message: 'Решение принято' });

  } catch (error) {
    console.error('Error processing admin decision:', error);
    res.status(500).json({ error: 'Ошибка обработки решения' });
  }
});

// 6. Скачивание NDA документа
app.get('/api/download/:filename', async (req, res) => {
  try {
    const { filename } = req.params;
    const filePath = path.join('uploads', filename);
    
    // Проверяем существование файла
    await fs.access(filePath);
    
    res.download(filePath);

  } catch (error) {
    console.error('Error downloading file:', error);
    res.status(404).json({ error: 'Файл не найден' });
  }
});

// Вспомогательные функции

async function sendTelegramNotification(application, decision) {
  try {
    const message = `
🔔 *NDA ${decision === 'approved' ? 'СОГЛАСОВАНО' : 'ОТКЛОНЕНО'}*

📋 *Компания:* ${application.companyName}
🏢 *ИНН:* ${application.inn}
📅 *Дата:* ${new Date().toLocaleString('ru-RU')}
✅ *Решение:* ${decision === 'approved' ? 'Согласовано' : 'Отклонено'}
    `;

    await fetch(`https://api.telegram.org/bot${config.telegram.botToken}/sendMessage`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        chat_id: config.telegram.chatId,
        text: message,
        parse_mode: 'Markdown'
      })
    });

  } catch (error) {
    console.error('Error sending Telegram notification:', error);
  }
}

async function updatePipedriveWithDecision(application, decision) {
  try {
    const noteText = `
🔔 Финальное решение по NDA
Компания: ${application.companyName}
ИНН: ${application.inn}
Решение: ${decision === 'approved' ? 'СОГЛАСОВАНО' : 'ОТКЛОНЕНО'}

Заключение AI:
${application.analysis.summary}

Критические замечания:
${application.analysis.criticalIssues.map(issue => `• ${issue}`).join('\n')}

Дата решения: ${new Date().toLocaleString('ru-RU')}
    `;

    // Поиск организации и создание заметки (аналогично предыдущему методу)
    const searchResponse = await fetch(
      `${config.pipedrive.baseUrl}/organizations/search?term=${application.inn}&api_token=${config.pipedrive.apiKey}`
    );
    const searchData = await searchResponse.json();

    let organizationId = null;
    if (searchData.data && searchData.data.items.length > 0) {
      organizationId = searchData.data.items[0].item.id;
    }

    await fetch(
      `${config.pipedrive.baseUrl}/notes?api_token=${config.pipedrive.apiKey}`,
      {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          content: noteText,
          org_id: organizationId
        })
      }
    );

  } catch (error) {
    console.error('Error updating Pipedrive with decision:', error);
  }
}

// Создание директории для загрузок
fs.mkdir('uploads', { recursive: true }).catch(console.error);

app.listen(PORT, () => {
  console.log(`Server running on port ${PORT}`);
});

module.exports = app;